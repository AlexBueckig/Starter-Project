# Starter-Project
Starter Project for Websites including SCSS and TS compiling aswell as bundling via gulp. 

## Usage
Type `gulp` in terminal to start a dev server.

`gulp build` will compile, minify and bundle all the sources moving them to the dist folder.
